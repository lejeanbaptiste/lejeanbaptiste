FROM node:18.1-alpine

# Needed because some dependencies are fetch from git and alpine does not come with git
RUN apk add --no-cache git

WORKDIR /app

COPY . .

CMD [ "npm", "--prefix", "packages/standalone", "run", "dev:docker-server" ]

EXPOSE 3000