/** Desktop app display name (matches `APP_NAME` in apps/desktop). */
export const DESKTOP_APP_DISPLAY_NAME = 'Le Jean-Baptiste';

/** Machine identifier for TEI `application/@ident` (matches bundle id slug). */
export const DESKTOP_APP_IDENT = 'le-jean-baptiste';
