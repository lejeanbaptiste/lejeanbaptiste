{
  "name": "grognard-entity-sync-worker",
  "version": "0.1.0",
  "description": "Cross-device entity sync backend for Grognard: a Cloudflare Worker over D1. Server-authoritative revisions, single-owner GitHub-token auth, pull/push endpoints.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.124.0"
  }
}
